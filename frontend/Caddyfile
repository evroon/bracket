:3000 {
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy {$BACKEND_URL:bracket-backend:8400}
    }
    handle {
        root * /app/dist
        file_server
        try_files {path} /index.html
    }
}
